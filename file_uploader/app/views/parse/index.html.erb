<h1>Parse#index</h1>
<p>Find me in app/views/parse/index.html.erb</p>

<%= @parse %>

<%= form_with(url: parse_show_path, local: true) do |form| %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
